---
const { title, id } = Astro.props;
---

<h1 id={id}>{title}</h1>

<style>
  h1 {
    display: block;
    text-align: center;
    font-size: var(--title-font-size);
    color: var(--title-font-color);
  }
</style>
