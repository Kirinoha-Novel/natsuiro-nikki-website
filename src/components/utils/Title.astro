---
const { title } = Astro.props;
---

<h1>{title}</h1>

<style>
  h1 {
    text-align: center;
    font-size: clamp(2rem, 6cqw, 2.7rem);
    color: var(--note-title-color);
    margin: 0 0 60px 0;
  }
</style>
